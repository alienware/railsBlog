<nav class="navbar navbar-default" role="navigation">
	<div class="container-fluid">
		<div class="navbar-header">
			<button type="button" class="navbar-toggle" data-toggle="collapse" data-target="#navbar-collapse">
				<span class="sr-only">Toggle navigation</span>
				<span class="icon-bar"></span>
				<span class="icon-bar"></span>
				<span class="icon-bar"></span>
			</button>
			<a class="navbar-brand" href="javascript:void(0)">la Confluence of Oceanus and Styx</a>
		</div>

		<div class="collapse navbar-collapse" id="navbar-collapse" style="float: right">
			<ul class="nav navbar-nav">
				<li class="<%= 'active' if current_page?(root_url) %>"><a href="/">Home</a></li>
				<li><a href="#">About me</a></li>
				<li><a href="#">Contact</a></li>
			</ul>
			<ul class="nav navbar-nav">
				<% if user_signed_in? %>
					<li class="<%= 'active' if params[:controller] == 'devise/registrations' %>">
						<%= link_to('Edit account', edit_user_registration_path) %>
					</li>

					<li>
						<%= link_to('Logout', destroy_user_session_path, :method=>'delete') %>
					</li>
					<li class="btn btn-lg btn-info">
						Signed in as <%= current_user.email %>
					</li>
				<% else %>
					<li>
						<%= button_to('Login', new_user_session_path, :class => 'btn btn-danger btn-lg')  %>
					</li>
				<% end %>
			</ul>
		</div>
	</div>
</nav>